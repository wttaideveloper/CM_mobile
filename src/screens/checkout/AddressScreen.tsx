import { StaticAddressScreen } from '@/screens/checkout/StaticAddressScreen';

/** Static checkout until the new cart APIs are wired. */
export function AddressScreen() {
  return <StaticAddressScreen />;
}
